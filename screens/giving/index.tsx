import React, { FC } from 'react';
import { SafeAreaView, Text, View } from 'react-native';

const Giving: FC = () => {
	return (
		<SafeAreaView>
			<View>
				<Text>Giving</Text>
			</View>
		</SafeAreaView>
	);
};
export default Giving;
